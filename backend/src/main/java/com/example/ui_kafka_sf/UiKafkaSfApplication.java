package com.example.ui_kafka_sf;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
/** UiKafkaSfApplication is part of the application's domain logic layer. */
public class UiKafkaSfApplication {

  /** Performs a unit of domain logic; see README for the surrounding flow. */
  public static void main(String[] args) {
    SpringApplication.run(UiKafkaSfApplication.class, args);
  }
}
